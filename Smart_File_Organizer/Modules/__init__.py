"""
modules/__init__.py
====================
Marks `modules/` as a Python package so that imports like
    from modules.logger_util import get_logger
resolve correctly regardless of the working directory.

No logic lives here — the file intentionally stays empty.
"""
